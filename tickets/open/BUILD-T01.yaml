---
acceptance_criteria: output/BUILD-T01-smoke.txt exists and contains 'tree-sitter-languages
  OK'
allowed_tools:
- exec_python
- write_file
attempts: 0
context_files:
- requirements.txt
- sample.m
depends_on: []
gate_command: pytest -q tests/build/test_BUILD_T01.py
id: BUILD-T01
max_retries: 2
result_path: output/BUILD-T01-smoke.txt
status: open
task_steps:
- 'Run: pip install -r requirements.txt'
- 'Run this Python snippet via exec_python and confirm no ImportError: from tree_sitter_languages
  import get_language, get_parser lang = get_language(''mumps'') parser = get_parser(''mumps'')
  tree = parser.parse(b''HELLO ; test'') assert tree.root_node.type != ''ERROR'',
  ''root node is ERROR'' print(''OK: tree-sitter-languages mumps grammar loads'')'
- 'Write a one-line smoke result to output/BUILD-T01-smoke.txt: ''tree-sitter-languages
  OK'''
title: Verify tree-sitter-languages installs and MUMPS grammar loads
---
