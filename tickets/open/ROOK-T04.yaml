---
acceptance_criteria: output/ROOK-T04-calls.yaml exists, parses as valid YAML, contains
  calls list, each entry has caller, callee, call_type, line keys
allowed_tools:
- read_file
- write_file
- exec_python
attempts: 0
context_files:
- routines/ORQQPL1.m
- output/ROOK-T01-ast.json
depends_on:
- ROOK-T01
gate_command: pytest -q tests/rook/test_ROOK_T04.py
id: ROOK-T04
max_retries: 2
result_path: output/ROOK-T04-calls.yaml
status: open
task_steps:
- Read output/ROOK-T01-ast.json
- Run tools/mumps/extract_calls.py against routines/ORQQPL1.m using exec_python
- 'Write result as YAML with frontmatter to output/ROOK-T04-calls.yaml; top-level
  key must be calls (list of dicts with keys: caller, callee, call_type, line)'
title: Extract cross-routine call graph from ORQQPL1.m
---
