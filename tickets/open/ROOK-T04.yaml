---
id: ROOK-T04
title: "Extract cross-routine call graph from ORQQPL1.m"
status: open
attempts: 0
max_retries: 2
depends_on:
  - ROOK-T01
context_files:
  - routines/ORQQPL1.m
  - output/ROOK-T01-ast.json
allowed_tools:
  - read_file
  - write_file
  - exec_python
task_steps:
  - "Read output/ROOK-T01-ast.json"
  - "Run tools/mumps/extract_calls.py against routines/ORQQPL1.m using exec_python"
  - "Write result as YAML with frontmatter to output/ROOK-T04-calls.yaml; top-level key must be calls (list of dicts with keys: caller, callee, call_type, line)"
gate_command: "pytest -q tests/rook/test_ROOK_T04.py"
result_path: output/ROOK-T04-calls.yaml
acceptance_criteria: "output/ROOK-T04-calls.yaml exists, parses as valid YAML, contains calls list, each entry has caller, callee, call_type, line keys"
---
