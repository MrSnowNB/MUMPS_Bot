---
acceptance_criteria:
- output/hello_message.py is created and syntactically valid Python
- Every non-blank, non-comment line has a
- All unresolvable constructs emit
- No dynamic XECUTE or @ indirection is silently translated
- Stub is generated by rule application only, not model inference
attempts: 0
context_files:
- output/hello_message.ir.json
- tools/mumps/ir_models.py
- .clinerules/05-tools.md
depends_on:
- PIPE-T04
description: 'Implement tools/mumps/emit_python_stub.py. Input: IR JSON from PIPE-T04.

  Output: Python stub file where every non-blank line carries a

  # MUMPS line N comment linking it to source. Emit TODO markers

  at any point requiring human verification. No freeform LLM-authored

  semantics may appear in the stub.

  '
gate: result.stub_exists == true and result.trace_coverage >= 0.95 and result.no_llm_content
  == true
result_path: output/hello_message.py
rule_ids_used:
- RULE-WRITE-001
- RULE-QUIT-001
- RULE-LABEL-001
status: open
ticket_id: PIPE-T05
title: Emit Python stub from minimal IR with line-trace comments
tool: python
tool_args:
- scripts/run_tool.py
- tools/mumps/emit_python_stub.py
- output/hello_message.ir.json
---
